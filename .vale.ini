StylesPath = .github/styles
MinAlertLevel = error
Packages = proselint

# Spelling is handled by codespell (see .codespellrc); Vale is used only for
# grammar and prose-style checks, so the built-in Vale.Spelling/Repetition rules
# are intentionally left out of BasedOnStyles.
[*.md]
BasedOnStyles = proselint
